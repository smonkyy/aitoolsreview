---
interface Props {
	id: string;
	title: string;
	description: string;
	pubDate: Date;
	readTime: number;
	gradient: string;
}

const { id, title, description, pubDate, readTime, gradient } = Astro.props;

const dateStr = pubDate.toLocaleDateString('it-IT', {
	day: 'numeric',
	month: 'short',
	year: 'numeric',
});
---

<a
	href={`/blog/${id}/`}
	class="group block rounded-2xl border overflow-hidden transition-all duration-300 no-underline
		bg-light-card border-light-border
		hover:border-purple-300/60 hover:shadow-xl hover:-translate-y-1
		dark:bg-ai-card dark:border-ai-border
		dark:hover:border-ai-purple/40 dark:hover:shadow-[0_16px_48px_rgba(168,85,247,0.15)]"
>
	<!-- Thumbnail: OG image (generated at build time) -->
	<div class="relative h-44 overflow-hidden bg-ai-bg2">
		<img
			src={`/og/${id}.png`}
			alt={title}
			width="600"
			height="315"
			class="w-full h-full object-cover object-top transition-transform duration-500 group-hover:scale-105"
			loading="lazy"
			decoding="async"
		/>
		<!-- Bottom gradient for badge legibility -->
		<div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent pointer-events-none"></div>
		<div class="absolute bottom-3 left-3">
			<span class="px-2.5 py-1 text-xs font-semibold rounded-full bg-ai-purple/90 text-white backdrop-blur-sm border border-ai-purple/30">
				Recensione
			</span>
		</div>
	</div>

	<!-- Content -->
	<div class="p-5">
		<div class="flex items-center gap-2 text-xs text-light-faint dark:text-ai-faint mb-2.5">
			<span>{dateStr}</span>
			<span class="w-1 h-1 rounded-full bg-current opacity-50 shrink-0"></span>
			<span>{readTime} min di lettura</span>
		</div>

		<h3
			class="font-bold text-sm leading-snug mb-2 line-clamp-2
				text-light-text dark:text-ai-text group-hover:text-ai-purple transition-colors duration-150"
		>
			{title}
		</h3>

		<p class="text-xs leading-relaxed line-clamp-2 text-light-muted dark:text-ai-muted">
			{description}
		</p>

		<div class="mt-4 pt-3.5 border-t border-light-border dark:border-ai-border flex items-center justify-between">
			<span
				class="text-xs text-ai-purple font-semibold inline-flex items-center gap-1
					group-hover:gap-2 transition-all duration-150"
			>
				Leggi la recensione
				<svg class="w-3 h-3 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
					<path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
				</svg>
			</span>
			<span class="text-xs text-light-faint dark:text-ai-faint">{readTime} min</span>
		</div>
	</div>
</a>
